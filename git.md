git init
#科隆
git clone 项目
#创建并切换到dev分支
git switch -c dev
#切换到dev分支
git switch dev
#查看当前分支
git branch
#切换分支
git switch master
#添加文件
git add readme.txt
#提交文件
git commit -m "message"
#合并dev分支到master
git merge dev
#删除dev分支
git branch -d dev
